/**
 * Synchronisation Pennylane -> Supabase.
 *
 * Les endpoints/filtres exacts doivent être implémentés après validation métier
 * et vérification de la documentation officielle Pennylane en vigueur.
 */
export async function syncPennylaneData() {
  throw new Error('syncPennylaneData non implémenté: validation métier et endpoints Pennylane requis')
}
